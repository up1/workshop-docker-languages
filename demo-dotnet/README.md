## Steps to run with [dotnet command](https://docs.microsoft.com/en-us/dotnet/core/tools/dotnet-new) ?
```
$dotnet restore
$dotnet publish -o ./published
$dotnet ./published/demo-dotnet.dll
```

Access to api = http://localhost/weatherforecast
